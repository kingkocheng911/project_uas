import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFE60012);    // Merah MepuPoin
  static const Color secondary = Color(0xFFF8F9FA);  // Background Light Gray
  static const Color accent = Color(0xFF1E88E5);     // Blue Accent
  static const Color textMain = Color(0xFF212529);   // Teks Utama
  static const Color textMuted = Color(0xFF6C757D);  // Teks Abu-abu
  static const Color white = Colors.white;
}